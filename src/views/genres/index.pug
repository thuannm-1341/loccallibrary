extends ../layout

block content
  h1 #{t(title)}
  if genres.length
    ul 
      each genre in genres 
        li 
          a(href=genre.url()) #{genre.name} 
  else
    p #{t('genre.title.emptyGenreList')}
